import React from 'react';
import ReactDOM from 'react-dom/client';
import ResizeLayout from './resize-layout/js/resize-layout';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ResizeLayout/>
  </React.StrictMode>
);

